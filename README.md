# LossProof (рабочее название)

Статический сайт (обычные HTML/CSS/JS, без сборки) для сравнения крипто-
маршрутов вывода выплат (биржа для покупки USDT + off-ramp сервис для
конвертации в рубли/локальную валюту) для трейдеров пропфирм из России и
СНГ. Банковский перевод оставлен как контрастный "худший" вариант.

Изначально собран по двум ТЗ (v1 и v2 — пивот с Wise/Revolut/банка на
крипто-маршруты, потому что Wise и Revolut не работают с резидентами России
из-за санкций). Файлы ТЗ удалены из репозитория 2026-09-23 — они остались в
истории git.

Сайт чисто информационный: **партнёрских и реферальных ссылок нет** (решение
владельца 2026-09-23), пропфирмы не оцениваются и не рекомендуются, в текстах
нет названий монет, прогнозов и цифр доходности. Как это объясняется
читателю — страница `/about/`.

## Запуск локально

Node.js/Python не требуются. В `scripts/serve.ps1` лежит небольшой
статический сервер на PowerShell (использует `System.Net.HttpListener`,
ничего устанавливать не нужно):

```powershell
powershell -ExecutionPolicy Bypass -File scripts\serve.ps1
```

Затем откройте http://localhost:8080/. (Открывать HTML-файлы напрямую через
`file://` не получится — страницы используют пути от корня сайта вроде
`/assets/...`, чтобы работать так же, как и после публикации.)

## Публикация

Сайт публикуется на Vercel автоматически при каждом пуше в `main` (сборки
нет, раздаётся корень репозитория). Служебные файлы — `*.md`, `scripts/`,
`.github/`, `.claude/` — исключены из публикации через `.vercelignore`;
новые служебные файлы кладите туда же или добавляйте в этот список.
Заголовки безопасности (CSP и др.) — в `vercel.json`.

## Структура

Сайт двуязычный: русская версия лежит в корне, английская — в `en/` с тем же
набором страниц (например `/payout/ftmo/` и `/en/payout/ftmo/`). В шапке
каждой страницы есть переключатель языка (иконка 🌐), который ведёт на
соответствующую страницу на другом языке.

- `index.html` / `en/index.html` — главная страница: заголовок-обещание
  («путеводитель в мире трейдинга»), плитка-хаб (`.hub-grid` в
  `style.css`) с карточками трёх категорий («Крипто и вывод денег»: калькулятор, гид,
  биржи, обменники; «Инструменты трейдера»: стоимость сделок, подбор стратегии;
  «Справочник»: вывод по фирмам, красные флаги, словарь) и блок
  «Почему LossProof» с перечислением сильных сторон сайта. Виджет
  калькулятора здесь больше не встроен напрямую — только карточка-ссылка на
  `/calculator/`. В шапке этой страницы (и только этой) навигация свёрнута
  до одной ссылки на «О проекте» — остальные разделы вынесены в
  плитку ниже заголовка; на всех остальных страницах сайта полная навигация
  сохранена.
- `calculator/index.html` / `en/calculator/index.html` — «Калькулятор
  переводов» (URL и код-идентификаторы всё ещё `calculator`/`calc-*` —
  переименовано только то, что видит пользователь): два поля — «Откуда» (у
  конкретной пропфирмы / на карте в любой валюте / уже в крипте, в том
  числе на конкретной бирже) и «Куда» (на конкретную биржу / сравнить все
  биржи / на оплату челленджа у конкретной пропфирмы / в конкретную валюту
  через off-ramp) — свободно комбинируются в любом направлении, а не только
  «пропфирма → валюта». Работает и как калькулятор вывода выплаты, и как
  калькулятор оплаты челленджа криптой (актуально для трейдеров из РФ/СНГ,
  которым карта может не дойти до западного мерчанта напрямую). Подробности
  логики см. в разделе «Как считает виджет» ниже. Блок с гридом всех фирм
  отсюда убран в отдельный раздел — см. `payouts/` ниже; здесь осталась
  только одна ссылка на него.
- `payouts/index.html` / `en/payouts/index.html` — раздел «Вывод по фирмам»:
  список всех пропфирм на сайте (грид из `FIRMS`, тот же компонент
  `.firm-grid`, что и в других местах) со ссылкой на инструкцию по выводу
  для каждой. Раньше кнопка «Вывод по фирмам» в навигации вела на
  `/calculator/#firms` — теперь это отдельная страница с полным списком, не
  привязанная к калькулятору.
- `payout/<slug-фирмы>/index.html` / `en/payout/<slug>/index.html` — страница
  на каждую пропфирму: способы вывода, комиссия, минимальная сумма,
  скорость, встроенный калькулятор переводов (открывается сразу с этой
  фирмой в поле «Откуда» — `presetSource: "firm:<slug>"` в вызове
  `initCalculator`), блок «Прежде чем регистрироваться» (нейтральное предупреждение: сайт не оценивает и не рекомендует пропфирмы, ссылка на список Банка России) и грид «Другие
  инструкции по выводу». Есть страницы для всех 11 фирм в `FIRMS`: ftmo,
  fundednext, the5ers, e8-markets, topstep, brightfunded, blueberry-funded,
  fundingpips, alpha-capital, fxify, funded-trading-plus. Три последние
  добавлены 2026-09-20 только по официальным страницам самих фирм; Россия и
  Беларусь у них в списках недоступных стран (на страницах об этом сказано
  прямо), а `challengePayment: null` означает, что фирма не предлагается в
  калькуляторе как «Куда» (условия оплаты челленджа криптой не подтверждены).
- `crypto-payout-guide/index.html` / `en/crypto-payout-guide/index.html` — гид по крипто-маршрутам вывода
- `exchanges/index.html` / `en/exchanges/index.html` — раздел «Сравнение
  бирж»: фильтруемая и сортируемая карточная сетка (та же логика, что у
  сравнения пропфирм — см. ниже) проверенных лицензированных криптобирж по
  лицензиям, доступу для резидентов РФ, комиссии спот-торговли и способам
  пополнения. Только площадки с публично проверяемой лицензией/
  VASP-регистрацией; WhiteBIT и HTX (Huobi) намеренно исключены — см. блок
  «Как мы отбираем биржи» на странице.
  Отдельных страниц по биржам нет (убраны 2026-09-25): все факты — в
  одинаковых карточках общего сравнения, без ссылок на сайты бирж. Старые
  адреса `/exchanges/<slug>/` перенаправляются на `/exchanges/` (`redirects`
  в `vercel.json`).
- `app/index.html` / `en/app/index.html` — страница нашего собственного
  приложения LossProof для учёта личных финансов (в разработке, на старте
  бесплатное). Это единственный продукт, который сайт продвигает; блок о нём
  есть и на главной, ссылка «Приложение» — в шапке всех страниц.
- `offramps/index.html` / `en/offramps/index.html` — раздел «Сравнение
  обменников» (то, что в коде и комментариях называется off-ramp —
  сервис, который меняет USDT на рубли): карточки по каждому сервису
  (лицензия, какую крипту принимает, в какие валюты выводит, комиссия,
  скорость). Пока 2 сервиса — Whitebird и Cifra Markets, те же, что
  использует калькулятор (`OFFRAMPS` в data.js). A7A5 сюда намеренно не
  включён — см. блок «Как мы отбираем обменники» на странице и комментарий
  над `OFFRAMPS` в файле данных.
- `terms/index.html` / `en/terms/index.html` — простыми словами о терминах,
  которые встречаются на сайте (просадка, профит-сплит, спред, P2P,
  обменник и т.д.), сгруппировано по темам. Статичная страница без
  JS-виджета и без своего файла данных — текст прямо в HTML, как на
  `about/`.
- `red-flags/index.html` / `en/red-flags/index.html` — «Красные флаги
  пропфирмы»: признаки скам-фирмы (проблемы с выплатами, непрозрачные
  правила, накрученные отзывы) и чек-лист, как проверить фирму самому.
  Тоже статичная страница без своих данных — использует тот же класс
  `.flag-list`, что и `.term-list` на `/terms/`, только с красным
  акцентом и иконкой 🚩. Раздел «Наш подход» прямо говорит, что сайт пропфирмы
  не оценивает: критерии — чек-лист читателя, а не вердикт сайта.
- `strategy/index.html` / `en/strategy/index.html` — квиз «Подбор стратегии»
  (с 2026-09-23): 12 вопросов по одному, результат — 1–3 стратегии работы с
  криптовалютой из 12 (долгосрочное удержание, DCA, свинг на споте и на
  фьючерсах с низким плечом, дейтрейдинг, скальпинг, сеточный/DCA/
  ребалансирующий боты — только описание, копитрейдинг, Earn, арбитраж ставки
  финансирования) и список инструментов, которые для неё нужны от площадки
  (демо-счёт, боты, автопокупка и т. п.). Биржи квиз не называет и не
  подбирает (с 2026-09-25). Прямая
  формулировка «вам подходит», но с защитой: стоп-экраны (младше 18,
  кредитные средства/подушка), жёсткие фильтры до подсчёта баллов (плечо —
  только при опыте, согласии на плечо, спокойной реакции на просадку и не
  «большая часть сбережений»), плашка «не является индивидуальной
  инвестиционной рекомендацией», список исключённых стратегий с причинами.
  Без названий монет, прогнозов и цифр доходности (правила рекламы
  криптовалют с 01.09.2026). Партнёрских ссылок нет (решение владельца
  2026-09-23), бирж в результате нет — только ссылка на нейтральное
  сравнение. Ответы считаются в браузере и никуда не отправляются.
- `costs/index.html` / `en/costs/index.html` — «Стоимость сделок» (с
  2026-09-23): человек загружает CSV-выгрузки истории сделок с биржи, и
  страница считает комиссии, фандинг, ликвидации, комиссии по опционам, обмен
  монет и проценты за заём, долю тейкерских сделок, фактические ставки против
  опубликованных, порог безубыточности по закрытым позициям и «те же сделки
  при других ставках» (ставки можно править прямо в таблице). Файлы
  обрабатываются только в браузере и никуда не отправляются — сервера у
  инструмента нет, персональных данных мы не получаем. Логика и разбор файлов
  — `assets/js/costs-core.js` (без DOM), интерфейс — `assets/js/costs.js`,
  демо-файлы с вымышленными инструментами COINA/COINB/COINC —
  `assets/data/costs-demo/` (формат 1:1 как у Bybit). Форматы: Bybit — сделки
  спот/деривативы, Closed P&L, журнал счёта (проверено на реальных выгрузках);
  OKX — история сделок и позиций (бета, по официальной справке, на реальном
  файле не проверено); любой другой CSV — ручным сопоставлением колонок.
  Чтобы не посчитать одно и то же дважды (у Bybit комиссия сделки есть и в
  истории сделок, и в журнале, и в Closed P&L), для каждой биржи и категории
  берётся самый точный файл (`COSTS_PRIORITY`), остальные — только за
  периоды вне его покрытия; одинаковые строки из разных файлов считаются один
  раз. Опубликованные ставки бирж для сравнения — `COSTS_FEE_TABLE` в
  `costs-core.js` (Bybit, BingX, MEXC сверены с официальными страницами
  2026-09-23; OKX-фьючерсы, KuCoin, Gate — по обзорам, помечены «не
  сверено»). После любых правок запускайте `scripts/costs-check.js` в консоли
  на `/costs/`: образцы всех форматов с посчитанными вручную суммами, правило
  «не дважды», пример и проверка экранирования. Для локальной проверки на
  своих выгрузках кладите их в `_private/` — папка в `.gitignore`.
- `about/index.html` / `en/about/index.html` — «О проекте и данных» (с
  2026-09-23, заменила «Раскрытие партнёрской монетизации» на `/disclosure/`):
  что партнёрств нет, как считают калькулятор и квиз, откуда данные,
  еженедельная проверка компаний, закон № 282-ФЗ (якорь `#law-2027`) и
  «не является индивидуальной инвестиционной рекомендацией».
- **Плашка о законе № 282-ФЗ** (`.law-note` в `style.css`) — сразу под hero
  на калькуляторе, гиде по крипто-выводу, сравнениях бирж и обменников, всех
  страницах бирж, «Выводе по фирмам», всех `/payout/<slug>/` и «Стоимости
  сделок» (RU и EN):
  с 01.07.2027 резиденты РФ смогут совершать сделки с цифровой валютой только
  через организаторов обращения (ст. 30), биржи и обменники на сайте —
  иностранные. Это временная нейтральная мера: когда станет известно, кто
  получит статус организатора обращения, маршруты гидов, калькулятора и
  квиза нужно пересобрать, а плашку — переписать. Квиз показывает такую же
  заметку при выборе «Россия» (`QUIZ_STRINGS.*.notes.ru`).
- `assets/js/data.js` — редактируемые данные для русской версии: валюты,
  биржи для расчёта (`EXCHANGES` — все 6 бирж с /exchanges/: bybit, bingx,
  kucoin, okx, mexc, gate), off-ramp
  сервисы (`OFFRAMPS` — у обоих `onRampSupported: true`: калькулятор
  предлагает их и как альтернативу бирже для покупки крипты, но тариф на
  покупку отдельно не проверялся, такие строки всегда помечены «не
  подтверждено»), банк-baseline (`BANK_BASELINE`), электронные кошельки и
  справочные ресурсы (`E_WALLETS`, `REFERENCE_RESOURCES` — не участвуют в
  расчёте, только в тексте статьи), данные по выплатам
  пропфирм (`FIRMS`), данные для сравнения бирж (`EXCHANGES_COMPARE` — отдельный, более широкий
  информационный обзор для страниц /exchanges/, не путать с `EXCHANGES`
  выше; см. комментарий над `EXCHANGES_COMPARE` в файле)
- `assets/js/data.en.js` — то же самое для английской версии. **При смене
  цифр (спред, комиссии, лимиты, slug фирмы) нужно обновить оба файла** —
  тексты можно переводить независимо, но числа должны совпадать.
- `assets/js/rates.js` — актуальные межбанковские курсы через бесплатный
  ExchangeRate-API (open.er-api.com; Frankfurter не подошёл — в нём нет
  рубля и других валют СНГ), общий для обеих версий
- `assets/js/strategy-quiz.js` — квиз «Подбор стратегии»: вопросы
  (`QUIZ_QUESTIONS`), правила и баллы стратегий (`STRATEGY_RULES`, жёсткие
  фильтры — `ineligibleReason`), что каждой стратегии нужно от площадки
  (`STRATEGY_NEEDS`, подписи — `QUIZ_STRINGS[lang].needs`), тексты
  интерфейса RU/EN (`QUIZ_STRINGS`). Один источник логики для обоих языков;
  тексты самих стратегий — `STRATEGIES` в `data.js` / `data.en.js`. После
  любых правок правил запускайте `scripts/strategy-quiz-check.js` в консоли
  на `/strategy/`: он перебирает все ~737 тыс. сочетаний ответов и проверяет
  правила безопасности, полноту текстов, что в квизе нет названий бирж, и
  сквозной проход по интерфейсу.
- `assets/js/exchanges.js` — то же самое для раздела «Сравнение бирж»:
  `initExchangesTable` (одинаковые карточки со всеми фактами о бирже, фильтр
  по доступу для РФ и select для сортировки; без ссылок на сайты бирж).
- `assets/js/offramps.js` — виджет раздела «Сравнение обменников»:
  `renderOfframpsCompare` строит карточки прямо из `OFFRAMPS` (тот же
  массив, что считает калькулятор) — отдельного файла данных для этого
  раздела нет. Всего 2 обменника, поэтому без фильтров и сортировки —
  только карточки.
- `assets/js/calculator.js` — виджет «Калькулятор переводов», общий для
  всех страниц (id корневого элемента и функция всё ещё называются
  `calculator-root`/`initCalculator` по историческим причинам). Источник и
  назначение — два независимых поля, оба выбираются свободно (не жёсткая
  пара «пропфирма → валюта»): **откуда** (`source`) — у конкретной
  пропфирмы (`firm:<slug>`, открывает карточку с комиссией/сроками/
  способами вывода этой фирмы из `FIRMS`), на карте в любой валюте
  (`cash:<CUR>`), либо уже в крипте — без привязки к бирже (`crypto`) или
  на конкретной бирже (`crypto:<exchangeId>`); **куда** (`destination`) —
  на конкретную биржу (`crypto:<exchangeId>`), сравнить сразу все варианты
  — биржи И обменники (`crypto:all`), на оплату челленджа у конкретной
  пропфирмы (`firm:<slug>` — берёт `firm.challengePayment` из `FIRMS`) или
  вывести в конкретную валюту через off-ramp (`fiat:<CUR>`). При выборе
  «сравнить все варианты» (для покупки крипты или оплаты фирме) в список
  попадают и `EXCHANGES`, и `OFFRAMPS` с `onRampSupported: true` — вторые
  всегда с бейджем «не подтверждено» (см. комментарий над `OFFRAMPS` в
  `data.js`). Подробности расчёта — в разделе «Как считает виджет» ниже.
  Язык — по атрибуту `<html lang="ru">`/`<html lang="en">`, подписи — из
  `CALC_STRINGS` внутри файла.
- `assets/icons/favicon.svg` — фавикон (растущий график, фирменный градиент
  синий→зелёный). `favicon-16.png`/`favicon-32.png`/`apple-touch-icon.png` —
  растровые версии для браузеров и iOS, подключены на всех страницах через
  `<link rel="icon">`. Чтобы поменять дизайн: отредактируйте `favicon.svg` и
  перегенерируйте PNG — `scripts/upload.ps1` поднимает локальный приёмник
  (`http://localhost:8081`), на который можно POST'ить PNG прямо из
  консоли браузера (canvas → `toBlob` → `fetch`), не гоняя base64 через
  текстовые инструменты.

## Обратная связь — временно снята с сайта

Раздел `/feedback/` (`/en/feedback/`, форма через Formspree) был убран с
публичного сайта 2026-09-07 по решению владельца — не был обязательной
частью сайта. Рабочий код (HTML + `feedback.js` с реальным подключённым
`FEEDBACK_ENDPOINT`) сохранён локально на случай, если раздел вернут — не в
git-репозитории. Перед повторным включением стоит сначала закрыть пробел с
политикой обработки персональных данных (форма собирает email через
стороннего зарубежного обработчика, а отдельной страницы политики ПДн на
сайте пока нет).

### Как считает виджет «Калькулятор переводов»

`source` (откуда) и `destination` (куда) — независимые поля вида
`тип:значение` (`parseSource`/`parseDestination` в `calculator.js`
разбирают их на `{type, ...}`), поэтому пары не ограничены направлением
«пропфирма → валюта» — можно точно так же посчитать «карта в рублях →
конкретная биржа», «карта в рублях → оплата челленджа у пропфирмы» или
любую другую комбинацию:

- `source`: `firm:<slug>` (пропфирма — валюта берётся из
  `firm.payoutCurrency`, показывается информационная карточка с выводом),
  `cash:<CUR>` (карта/счёт в любой из `CURRENCIES`), `crypto` (уже в
  USDT/USDC, без привязки к бирже) или `crypto:<exchangeId>` (уже на
  конкретной бирже).
- `destination`: `crypto:<exchangeId>` (конкретная биржа), `crypto:all`
  (сравнить все биржи из `EXCHANGES`), `firm:<slug>` (оплатить челлендж
  этой пропфирмы криптой — берёт `firm.challengePayment` из `FIRMS`) или
  `fiat:<CUR>` (вывести в валюту через off-ramp).

Все комиссии в данных (`EXCHANGES`/`OFFRAMPS`/`BANK_BASELINE`) заданы в
долларах, поэтому расчёт всегда идёт через доллар как опорную точку:
сумма сначала переводится в USD-эквивалент через `getMidMarketRate(from,
"USD")`, если исходная валюта не USD и не крипта (`rateToUSD`), с неё
вычитаются комиссии и применяется спред, а результат переводится в валюту
назначения через `getMidMarketRate("USD", to)`, если это не USD и не
крипта/пропфирма (`rateFromUSD`). Это принципиально: раньше курс
запрашивался только для пары «funding-валюта → local-валюта» напрямую и
никогда не пересчитывался, если направление было «в крипту» — из-за этого
маршрут вида «рубли на карте → купить USDT на Bybit» до этой переделки
считался неверно (курс молча принимался за 1, то есть 100 000 ₽
отображались как ~99 700 USDT вместо реальных ~1 180 USDT).

`runCalculation` дальше ветвится по типам `source`/`destination`
(`isDestCryptoLike = isDestCrypto || isDestFirm` — обе ветки заканчиваются
в USDT, без второго/off-ramp этапа):

1. **`destination` — пропфирма, которая не принимает крипту**
   (`firm.challengePayment.acceptsCrypto === false`, сейчас это только
   Topstep): маршрута нет — сразу показывается предупреждение с честным
   объяснением (какой способ оплаты у фирмы принят на самом деле), без
   единого расчёта.
2. **`source` и `destination` — оба крипта**: переводить нечего. Если
   указана одна и та же конкретная биржа с обеих сторон — отдельное
   сообщение «уже там»; если биржи разные или не указаны — общее
   пояснение, что перевод между биржами эта версия виджета не считает (нет
   данных о комиссии такого перевода).
3. **валюта источника совпадает с валютой назначения** (оба фиат): то же
   самое — конвертация не нужна.
4. **`destination` — пропфирма, принимающая крипту**: этап биржи (если
   `source` — не крипта) плюс комиссия платёжного провайдера фирмы за
   приём крипты, если она известна (`buildFirmPaymentRow`; сейчас это
   только FTMO — официальные 3%, у остальных помечено как
   `dataVerified: false` и показывается бейдж «не подтверждено»). Колонка
   «получите на руки» заменяется на «дойдёт до фирмы», под таблицей —
   карточка с принимаемыми монетами и прямой ссылкой на сайт фирмы. Если
   `source` не крипта, в качестве этапа покупки перебираются и все `EXCHANGES`
   (6 бирж), и все `OFFRAMPS` с `onRampSupported: true` (обменники) — вторые
   в названии строки помечены суффиксом «(обменник)» и всегда unverified,
   так как тариф на покупку у них отдельно не проверялся (переиспользуется
   подтверждённый тариф на продажу).
5. **`destination` — крипта (не пропфирма), `source` — фиат**:
   только этап биржи/обменника (`buildBuyRow`) — если `destination` —
   конкретная биржа (`crypto:<exchangeId>`), одна строка; если `crypto:all`,
   по одной строке на каждую из `EXCHANGES` плюс на каждый `OFFRAMPS` с
   `onRampSupported: true` (тот же принцип, что в пункте 4 — обменники
   помечены как unverified).
6. **`source` — крипта, `destination` — фиат**: только этап off-ramp
   (`buildOfframpOnlyRow`), без строки биржи и без банка (сравнивать
   банковский перевод не с чем, если деньги уже не в фиате).
7. **оба фиат, валюты разные**: полная матрица — по одной строке на каждую
   пару биржа×off-ramp (`buildRoute`) плюс строка `BANK_BASELINE` для
   контраста.

`FIRMS[].challengePayment` (принимает ли фирма оплату челленджа криптой)
собрано веб-поиском по официальным FAQ фирм 2026-09-12 — см. комментарии
над каждой записью в `data.js`/`data.en.js` для источников.
`cryptoFeePercent: null` там, где сама комиссия платёжного провайдера за
приём крипты не публикуется (FundedNext, The5ers, E8 Markets, BrightFunded,
Blueberry Funded, FundingPips) — виджет честно показывает это как
неизвестное, а не считает комиссию нулевой. У Topstep `acceptsCrypto:
false` — единственная из 8 фирм, которая принимает оплату только
банковской картой.

#### Диапазон вместо точного числа у обменников

По ТЗ `calculator-full-rebuild-spec.md` (папка `тех задания сайт/` рядом с
проектом, вне репозитория) — у off-ramp сервисов (Whitebird, Cifra Markets)
нет публичного API тарифов, их курс USDT↔fiat привязан к P2P-рынку и
колеблется, поэтому показывать одно точное число значило бы вводить в
заблуждение ложной точностью. Правило в `calculator.js`: **любой этап, где
участвует обменник** — как off-ramp (`buildOfframpOnlyRow`, `buildRoute`),
так и как альтернатива бирже для покупки (`buildBuyRow`/`buildFirmPaymentRow`
с `isOfframp: true`) — считается диапазоном `finalAmountMin`…`finalAmountMax`
(± `OFFRAMP_TOLERANCE_PERCENT` = 0,4%, середина диапазона «±0,3-0,5%» из ТЗ,
применяется к `spreadPercent` перед расчётом обеих границ). Этап биржи
(`EXCHANGES`) остаётся точным числом — ТЗ прямо говорит, что там колебания
минимальны. `BANK_BASELINE` (контрастный банковский вариант) тоже считается
диапазоном для единообразия — в ТЗ нет отдельных чисел под него, поэтому
используется тот же допуск.

Сортировка результатов — по **нижней границе** диапазона (`finalAmountMin`,
или `finalAmount`, если строка без диапазона), чтобы наверху была самая
консервативная оценка, а не самая оптимистичная. Под таблицей — блок
`rangeDisclaimer` появляется, только если хотя бы одна строка в текущих
результатах помечена `hasRange: true`; если весь маршрут состоит из точных
чисел (например, «карта в USD → конкретная биржа»), блок не показывается.

### Политика по пропфирмам (с 2026-09-20)

Сайт **не оценивает и не рекомендует пропфирмы** и не связан партнёрскими
отношениями ни с одной из них (партнёрские ссылки на пропфирмы не
планируются). Разделы «Сравнение пропфирм», «Подбор пропфирмы» и «Проверка
условий» удалены; остались нейтральные справочные страницы `/payout/<slug>/`
(способы вывода, комиссии, минимумы — только факты с датой сверки и ссылкой
на официальный сайт), общий материал `/red-flags/` и данные для калькулятора
(`FIRMS` в `data.js`/`data.en.js`). На каждой странице фирмы есть блок
«Прежде чем регистрироваться». Прежние исследования (рейтинги, цены, критерии
двух уровней) остались в истории git до коммита удаления. Все компании сайта
(пропфирмы, биржи, обменники) проверяются еженедельной автопроверкой
(`.github/workflows/compliance-check.yml`, `scripts/compliance-entities.json`,
Issues с меткой `compliance`). Проверка только сообщает — страницы не
меняются автоматически: совпадение названия может оказаться однофамильцем
или клоном, поэтому каждую находку разбирают вручную. Читателю о проверке
рассказано на `/about/`.

### Добавление нового языка/страницы

Проще всего скопировать существующую пару файлов (например
`payout/ftmo/index.html` и `en/payout/ftmo/index.html`) и отредактировать
текст — вся логика (калькулятор, курсы, список фирм) подключается через
`<script src="...">` и ничего дублировать в JS не нужно, кроме текстов в
`data.js`/`data.en.js`.

## Что нужно доделать перед запуском

- **Домен**: когда он будет куплен, подставьте его везде, где встречается
  `example.com` (`sitemap.xml`, `robots.txt`), и сделайте `canonical`/
  `hreflang` абсолютными.
- **Проверить данные по пропфирмам (вывод)**: на каждой странице фирмы в
  разделе `/payout/` есть красный блок «проверьте», отмечающий неподтверждённые
  цифры (минимальная сумма вывода FundedNext, противоречивые данные по
  комиссии The5ers, скорость E8 Markets) — сверьте их напрямую с фирмой перед
  тем как убирать эти блоки. Записи Topstep, BrightFunded и Blueberry Funded
  в `FIRMS` добавлены веб-поиском 2026-09-08 (источники — в комментарии над
  каждой записью в `data.js`/`data.en.js`) и тоже нужно сверить перед
  запуском.
- **Проверить данные для «Сравнения бирж»**: `EXCHANGES_COMPARE` в
  `assets/js/data.js` / `data.en.js` собран веб-поиском на дату
  `EXCHANGES_COMPARE_RESEARCHED` и НЕ сверялся построчно с официальными
  сайтами бирж — у всех 6 стоит `dataVerified: false`, под сравнением
  показывается пометка «сверьте данные». Особое
  внимание — статус `ruAccessTier`/`ruAccessText` (доступ для резидентов РФ):
  это самое изменчивое поле во всей таблице, у Bybit/OKX/Gate прямо
  противоречит их же пользовательскому соглашению. Перепроверяйте перед
  публикацией и периодически после — регулирование в России меняется быстро
  (ожидаются изменения летом 2026). Числа в `data.js` и `data.en.js` должны
  совпадать.
- **Тарифы обменников**: калькулятор берёт тарифы Whitebird и Cifra Markets
  из `assets/data/tariffs.json`, который каждую ночь (01:00 МСК)
  обновляет `.github/workflows/update-tariffs.yml` с официальных страниц
  тарифов. Упавший запуск значит, что сервис поменял вёрстку страницы и нужно
  поправить `scripts/tariffs-parse.mjs`. Страница `/offramps/` по-прежнему
  читает статичные поля `OFFRAMPS` в `data.js`/`data.en.js` — их сверяйте
  вручную. Курс валют обновляется при каждом расчёте через `rates.js`.
- **Изображение для соцсетей (OG-картинка)**: пока не добавлено (фавикон уже
  есть, см. ниже).

## Намеренно исключено

- **Всё, что читается как реклама сторонних сервисов** — убрано 2026-09-25
  после юридической проверки сайта: отдельные страницы бирж со ссылкой
  «сверьте перед регистрацией», кнопки «Перейти на сайт» у обменников,
  значок «Выгоднее всего» в калькуляторе, подбор бирж в квизе, ссылка на
  агрегатор BestChange, фразы о том, как резиденты РФ обходят ограничения
  бирж, упоминание VPN. Все компании показываются одинаково, в общих
  сравнениях; внешние ссылки — только как источники (`rel="nofollow"`).
  Продвигаем только собственное приложение (`/app/`). Не возвращать без
  нового юридического разбора.
- **P2P-маршруты в калькуляторе** — убраны 2026-09-25: P2P-сделки с
  частными продавцами для резидентов РФ связаны с блокировками карт по
  115-ФЗ и ответственностью за дропперство. P2P остаётся только как факт в
  способах пополнения бирж и как термин в словаре — с предупреждением.
- **Яндекс Метрика** — отключена 2026-09-25 до запуска: счётчик ставил
  cookie без согласия посетителя, а с 01.09.2025 для этого нужны согласие
  и политика обработки персональных данных (152-ФЗ). Вернуть можно
  вместе с баннером согласия и страницей политики: файл
  `assets/js/metrika.js` — в истории git (коммит 34e44fa), плюс домены
  Метрики в CSP (`vercel.json`) и в `scripts/allowed-domains.txt`.
- **Google Fonts** — с 2026-09-25 шрифты (Fraunces, Archivo, IBM Plex Mono)
  лежат на самом сайте: `assets/fonts/` + `assets/css/fonts.css`, лицензии OFL
  рядом. Так браузер посетителя не передаёт его IP в Google; из сторонних
  сервисов сайт обращается только к ExchangeRate-API.

- **Партнёрские и реферальные ссылки** — сняты полностью 2026-09-23 (список
  `AFFILIATE_LINKS`, флаги `affiliateConfirmed`, кнопка «Оформить» в
  калькуляторе, цель Метрики `affiliate_click`, проверка партнёрских ссылок в
  `scripts/check-links.sh`). Реклама иностранных криптобирж для аудитории из
  РФ закрыта законом «О рекламе» (ст. 7 п. 7, ст. 29.2), а ст. 30 закона
  № 282-ФЗ запрещает с 01.07.2027 информировать о способах сделок в обход
  организаторов обращения. Не возвращать без нового юридического разбора.
- **A7A5 — исключён из сайта.** Это не отдельный off-ramp, а рублёвый
  стейблкоин от A7/Old Vector LLC, торгуемый в основном на бирже Grinex.
  Grinex и связанные с A7A5 структуры попали под санкции OFAC Минфина США
  в августе 2025 года как преемник санкционного Garantex — репутационный
  и юридический риск, поэтому убран из `OFFRAMPS` и из всех текстов сайта.
  Не возвращать без повторной проверки санкционного статуса.
